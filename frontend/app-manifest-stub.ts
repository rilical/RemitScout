// Nuxt internal: used by a dynamic import("#app-manifest") that can be present in Nuxt's source.
// In dev, Vite may try to resolve it even if unreachable at runtime.
// This stub keeps the module resolvable in all environments.
export default {}

