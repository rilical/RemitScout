# Ignore the Golden Rule test file in normal lint runs (we will lint it separately in a controlled way)
plane-a-product/ForbiddenImport.test.js

# Ignore any secrets or local env files
secrets/**
*.env
