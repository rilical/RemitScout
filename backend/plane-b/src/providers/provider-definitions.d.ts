import type { ProviderDefinition } from './registry-builder';
/**
 * Single source of provider collector wiring and static metadata.
 */
export declare const providerDefinitions: ProviderDefinition[];
