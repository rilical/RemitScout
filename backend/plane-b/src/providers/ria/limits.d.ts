export declare const httpLimits: {
    rpm: number;
    concurrency: number;
    perLocale: boolean;
    perCorridorRpm: number;
};
/**
 * MVP: Playwright support is not yet implemented.
 * These limits are defined for future use and do not affect current functionality.
 */
export declare const playwrightLimits: {
    rpm: number;
    concurrency: number;
    perLocale: boolean;
    perCorridorRpm: number;
};
