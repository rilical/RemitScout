# Backend Documentation

This index covers the Markdown files scoped to `backend/`.

## Core Docs
- Shared directory boundaries: `shared/README.md`
- Plane B notifications subsystem: `plane-b/src/notifications/README.md`

## Maintenance Note
- These are the only backend-scoped Markdown files today. Add new docs here when created.
