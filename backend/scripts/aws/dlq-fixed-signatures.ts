export const DLQ_PURGE_ALLOWLIST: Record<string, string[]> = {
  // Populate queue-specific fingerprint hashes here only after confirming the
  // underlying bug is fixed and the messages are older than the purge threshold.
}
